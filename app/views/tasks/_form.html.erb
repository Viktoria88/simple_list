<%= form_for @task, remote: true do |f| %>
    <%= f.text_field :title, placeholder: "Start typing here to create a task..." %>
    <%= f.date_select :deadline %>
    <%= f.hidden_field :project_id, :value => @project_id %>
    <%= f.submit "Add Task" %>
<% end %>