<%= form_for @task, remote: true do |f| %>
    <%= f.text_field :content, placeholder: "Start typing here to create task..." %>
    <%= f.check_box :done %>
    <%= f.date :deadline %>
    <%= f.submit "Add Task"%>
<% end %>